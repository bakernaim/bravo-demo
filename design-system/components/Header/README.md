Store header: espresso promo strip, uppercase category nav, centred monogram, and search/account/wishlist/bag actions.

- `links` (nav labels; "Sale" renders in `sale`), `current` (underlined in gold), `bagCount`, `promo` (text, or `false` to hide the strip).
- Sticks to the top on scroll in the real site; collapse nav into a menu below 900px.
