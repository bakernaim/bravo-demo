4:5 product photo frame with a gold line-drawing placeholder until real photos exist.

- `src` + `alt` for a real photo (object-fit cover).
- `tone`: `soft` (gold-soft), `raised` (white with hairline), `espresso` — alternate tones across a grid for rhythm.
- `shape` picks the placeholder drawing: heel, flat, boot, sneaker.
- Children render on top (tags, wishlist button).
