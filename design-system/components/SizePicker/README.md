EU size selector as a radio group of square buttons, with a size-guide link.

- `sizes` (default 36–41), `soldOut` (array — shown hatched and disabled), `value`, `onChange(size)`.
- The selected size gets a 2px `ink` ring and is echoed in the label ("Size (EU) — 38").
