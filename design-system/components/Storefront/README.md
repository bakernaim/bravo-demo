The proof-of-concept home page, assembled from the system's components.

Header with promo strip → espresso hero with the monogram → Shop by category → New arrivals grid → service promises → footer with newsletter and the Instagram handle. Optional `currency` prefix for prices.
