Thin line icon on a 24px grid that inherits `currentColor`.

- `name`: search, bag, heart, user, arrow, truck, swap, star, ruler.
- Decorative by default; pass `label` when the icon stands alone without text. Icon-only buttons need an `aria-label` on the button.
